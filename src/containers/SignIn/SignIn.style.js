import styled from 'styled-components';
import tw from 'twin.macro';

const SignInStyleWrapper = styled.div`
  ${tw`flex justify-center items-center h-screen w-screen bg-gray-100`}
`;

export default SignInStyleWrapper;
